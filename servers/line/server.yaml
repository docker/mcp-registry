name: line
image: mcp/line
type: server
meta:
  category: devops
  tags:
    - line
    - devops
about:
  title: LINE
  icon: https://avatars.githubusercontent.com/u/13128444?v=4
source:
  project: https://github.com/line/line-bot-mcp-server
  commit: 78f3b4679c09ed2f3b4c69f42d6d0b4292cf8bbb
config:
  description: Configure the connection to LINE
  secrets:
    - name: line.access_token
      env: CHANNEL_ACCESS_TOKEN
      example: FILL_HERE
  env:
    - name: DESTINATION_USER_ID
      example: FILL_HERE
      value: "{{line.user_id}}"
  parameters:
    type: object
    properties:
      user_id:
        type: string
    required:
      - user_id
