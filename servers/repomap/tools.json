[
  {
    "name": "map",
    "description": "Start here. A map of the repository: modules (communities of files that depend on each other), the most central files (PageRank), the most used symbols, and entry points. Pass `focus` (a directory) to zoom in and get an outline of its files and symbols with line numbers.",
    "arguments": [
      {
        "name": "focus",
        "type": "string",
        "desc": "Directory to zoom into, e.g. src/server."
      },
      {
        "name": "format",
        "type": "string",
        "desc": "text (default, compact, file:line cited) or json."
      },
      {
        "name": "limit",
        "type": "integer",
        "desc": "Items per section (default 12)."
      },
      {
        "name": "root",
        "type": "string",
        "desc": "Repository root. Defaults to the client's workspace root or the server's working directory."
      }
    ]
  },
  {
    "name": "search",
    "description": "Hybrid code search: symbol names plus BM25 over AST chunks (whole functions, methods, classes). Use natural words or identifiers, e.g. \"refresh token expiry\" or \"parseConfig\". Returns ranked file:line ranges with the matching lines.",
    "arguments": [
      {
        "name": "format",
        "type": "string",
        "desc": "text (default, compact, file:line cited) or json."
      },
      {
        "name": "include_tests",
        "type": "boolean",
        "desc": "Default true (tests rank lower)."
      },
      {
        "name": "kind",
        "type": "string",
        "desc": ""
      },
      {
        "name": "limit",
        "type": "integer",
        "desc": "Max results (default 10)."
      },
      {
        "name": "path",
        "type": "string",
        "desc": "Only files whose path starts with or contains this."
      },
      {
        "name": "query",
        "type": "string",
        "desc": ""
      },
      {
        "name": "root",
        "type": "string",
        "desc": "Repository root. Defaults to the client's workspace root or the server's working directory."
      }
    ]
  },
  {
    "name": "context",
    "description": "Everything about one symbol or file: its code, who calls it (with call-site lines), what it calls, subtypes, members, imports, importers and the tests that touch it. Target forms: `path/to/file.ts`, `file.ts:42`, `Class.method`, `Class::method`, or a bare name.",
    "arguments": [
      {
        "name": "code_lines",
        "type": "integer",
        "desc": "Lines of source to include for a symbol (default 60, 0 for none)."
      },
      {
        "name": "format",
        "type": "string",
        "desc": "text (default, compact, file:line cited) or json."
      },
      {
        "name": "root",
        "type": "string",
        "desc": "Repository root. Defaults to the client's workspace root or the server's working directory."
      },
      {
        "name": "target",
        "type": "string",
        "desc": ""
      }
    ]
  },
  {
    "name": "trace",
    "description": "With `from` and `to`: the shortest call path between two symbols or files, each hop cited file:line (falls back to the file dependency path). With only `from`: the call tree below it (direction callees) or above it (direction callers).",
    "arguments": [
      {
        "name": "depth",
        "type": "integer",
        "desc": "Tree depth (default 3)."
      },
      {
        "name": "direction",
        "type": "string",
        "desc": ""
      },
      {
        "name": "format",
        "type": "string",
        "desc": "text (default, compact, file:line cited) or json."
      },
      {
        "name": "from",
        "type": "string",
        "desc": ""
      },
      {
        "name": "root",
        "type": "string",
        "desc": "Repository root. Defaults to the client's workspace root or the server's working directory."
      },
      {
        "name": "to",
        "type": "string",
        "desc": ""
      }
    ]
  },
  {
    "name": "impact",
    "description": "What breaks if this changes. Give `target` (symbol or file, or a list) or `changed: true` to analyse the current git diff. Returns a risk level, direct and indirect callers with call sites, importing files, modules touched, and the tests to run.",
    "arguments": [
      {
        "name": "base",
        "type": "string",
        "desc": "Git ref for `changed` (default HEAD)."
      },
      {
        "name": "changed",
        "type": "boolean",
        "desc": "Use the working-tree git diff (against `base`)."
      },
      {
        "name": "depth",
        "type": "integer",
        "desc": "Caller depth (default 3)."
      },
      {
        "name": "format",
        "type": "string",
        "desc": "text (default, compact, file:line cited) or json."
      },
      {
        "name": "root",
        "type": "string",
        "desc": "Repository root. Defaults to the client's workspace root or the server's working directory."
      },
      {
        "name": "target",
        "type": "string",
        "desc": ""
      }
    ]
  }
]