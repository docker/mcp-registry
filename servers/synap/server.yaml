name: synap
type: remote
dynamic:
  tools: true
meta:
  category: knowledge
  tags:
    - memory
    - knowledge
    - ai-agents
    - remote
about:
  title: Synap Memory
  description: Persistent memory for AI agents — log and recall conversation context over MCP.
  icon: https://avatars.githubusercontent.com/u/218974558?v=4
remote:
  transport_type: streamable-http
  url: https://synap-mcp.maximem.ai/mcp
  headers:
    Authorization: "Bearer ${SYNAP_API_KEY}"
config:
  secrets:
    - name: synap.api_key
      env: SYNAP_API_KEY
      example: <YOUR_SYNAP_API_KEY>
