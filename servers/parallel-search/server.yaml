name: parallel-search
type: remote
dynamic:
  tools: true
meta:
  category: search
  tags:
    - search
    - web-search
    - research
    - remote
about:
  title: Parallel Search
  description: LLM-optimized web search and URL extraction from Parallel Web Systems. Provides two tools — web_search (objective + queries → ranked excerpts) and web_fetch (URL → markdown content) — that return citation-ready text designed to be fed directly into an LLM with minimal post-processing.
  icon: https://assets.parallel.ai/dark-parallel-symbol-270.svg
remote:
  transport_type: streamable-http
  url: https://search.parallel.ai/mcp
