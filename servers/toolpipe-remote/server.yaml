name: toolpipe-remote
type: remote
dynamic:
  tools: true
meta:
  category: development
  tags:
    - developer-tools
    - json
    - uuid
    - hash
    - dns
    - pdf
    - code-review
    - qr-code
    - regex
    - jwt
    - fake-data
    - remote
about:
  title: ToolPipe
  description: |
    238+ developer tools via MCP: JSON formatting, QR code generation, hashing (MD5/SHA),
    UUID generation, DNS lookup, PDF tools, code review, fake data generation, regex testing,
    JWT decoding, Base64 encoding, HTML/Markdown conversion, color manipulation, cron parsing,
    network utilities, and much more.

    No API key required for the free tier.
  icon: https://toolpipe.dev/favicon.ico
remote:
  transport_type: streamable-http
  url: https://toolpipe.dev/mcp
