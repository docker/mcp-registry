name: one
type: remote
dynamic:
  tools: true
meta:
  category: tools
  tags:
    - tools
    - integrations
    - automation
    - api
    - remote
about:
  title: One
  description: Search, document and execute authenticated API calls across 700+ apps via one MCP server
  icon: https://www.withone.ai/icons/apple-touch-icon.png
remote:
  transport_type: streamable-http
  url: https://mcp.withone.ai/mcp
oauth:
  - provider: one
    secret: one.personal_access_token
    env: ONE_PERSONAL_ACCESS_TOKEN
