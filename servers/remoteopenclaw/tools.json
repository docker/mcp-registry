[
  {
    "name": "search_mcp_servers",
    "description": "Search Remote OpenClaw's directory of 13,870+ MCP servers for Claude Code, OpenClaw, Hermes, Codex, and Cursor. Returns names, descriptions, links, and popularity.",
    "arguments": [
      {
        "name": "query",
        "type": "string",
        "desc": "What to search for, e.g. 'postgres database'",
        "required": true
      },
      {
        "name": "limit",
        "type": "number",
        "desc": "Maximum number of results (1-25, default 10)"
      }
    ]
  },
  {
    "name": "search_skills",
    "description": "Search Remote OpenClaw's directory of 4,384+ agent skills for Claude Code, OpenClaw, Hermes, and Codex. Returns names, descriptions, links, and install commands.",
    "arguments": [
      {
        "name": "query",
        "type": "string",
        "desc": "What to search for, e.g. 'pdf' or 'react'",
        "required": true
      },
      {
        "name": "limit",
        "type": "number",
        "desc": "Maximum number of results (1-25, default 10)"
      }
    ]
  },
  {
    "name": "search_plugins",
    "description": "Search Remote OpenClaw's directory of Claude Code, OpenClaw, Hermes, and Codex plugins. Returns names, descriptions, and links.",
    "arguments": [
      {
        "name": "query",
        "type": "string",
        "desc": "What to search for, e.g. 'testing'",
        "required": true
      },
      {
        "name": "limit",
        "type": "number",
        "desc": "Maximum number of results (1-25, default 10)"
      }
    ]
  }
]
