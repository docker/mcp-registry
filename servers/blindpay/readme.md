Docs: https://blindpay.com/docs

Create an API key in the BlindPay dashboard (instance → API Keys). The same endpoint also accepts OAuth 2.1 sign-in from clients that support it; see https://github.com/blindpaylabs/blindpay-mcp.
