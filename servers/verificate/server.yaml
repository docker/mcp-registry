name: verificate
type: remote
dynamic:
  tools: true
meta:
  category: code
  tags:
    - code
    - code-quality
    - code-review
    - validation
    - remote
about:
  title: Verificate Gate
  description: The merge gate for AI-written code. Deterministic checks veto placeholder implementations, calls to APIs that do not exist, tests that cannot fail and unsupported "it's done" claims; a model review then grades what remains and returns a fix plan. No signup needed to start.
  icon: https://www.google.com/s2/favicons?domain=verificate.ai&sz=64
remote:
  transport_type: streamable-http
  url: https://mcp.verificate.ai/mcp
