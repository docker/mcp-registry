name: anyapi
image: mcp/anyapi
type: server
meta:
  category: web-scraping
  tags:
    - web-scraping
    - data
    - search
    - social-media
about:
  title: AnyAPI
  description: |-
    AnyAPI is a unified marketplace for scraping and data APIs: any API, one wallet, USD, no subscriptions.

    Reach hundreds of third-party APIs - social media, search results, maps, e-commerce, and general web data - through one key and one normalized interface. Every API exposes a normalized input/output JSON Schema, so an agent discovers, inspects, quotes, and runs any of them with the same loop.

    Pricing is real-dollar pay-per-request with no subscription. Discovery is free and failed calls are never charged, because AnyAPI fails over across upstream providers automatically under one price reservation.

    This server runs locally in a container and speaks MCP over stdio. Browsing the catalog, reading an API's schema, and quoting a price all work without a key; only running an API charges a wallet and needs one.
  icon: https://avatars.githubusercontent.com/u/292946935?s=200&v=4
source:
  project: https://github.com/getanyapi-com/mcp
  commit: d16b67424b64e4a80821e78cdc9bf1394499805b
config:
  description: Configure your AnyAPI access. The key is optional - discovery and pricing work without one, and only run_api needs it.
  secrets:
    - name: anyapi.api_key
      env: ANYAPI_API_KEY
      example: <YOUR_API_KEY>
      required: false
