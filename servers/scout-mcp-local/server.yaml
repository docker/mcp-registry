name: scout-mcp-local
image: scoutapp/scout-mcp-local
type: server
meta:
    category: monitoring
    tags:
        - monitoring
about:
    title: Scout Monitoring MCP
    description: Put Scout Monitoring's performance and error data directly in the hands of your AI Assistant. For Rails, Django, FastAPI, Laravel and more. Use it to get traces and errors with line-of-code information that the AI can use to target fixes right in your editor and codebase. N+1 queries, slow endpoints, slow queries, memory bloat, throughput issues - all your favorite performance problems surfaced and explained right where you are working.
    icon: https://avatars.githubusercontent.com/u/458509?v=4
source:
    project: https://github.com/scoutapp/scout-mcp-local
    commit: 493cdaffb970a1b090f945b122a462639ac1496d
config:
    description: Configure the connection to Scout Monitoring MCP
    secrets:
        - name: scout-mcp-local.api_key
          env: SCOUT_API_KEY
          example: <SCOUT_API_KEY>
    env:
        - name: foo
          example: bar
          value: '{{scout-mcp-local.foo}}'
    parameters:
        type: object
        properties:
            foo:
                type: string
