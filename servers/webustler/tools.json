[
  {
    "name": "scrape",
    "description": "Scrape a URL and return clean markdown with metadata, links, and images. Handles Cloudflare-protected sites automatically.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "The URL to scrape"
        }
      },
      "required": ["url"]
    }
  }
]
