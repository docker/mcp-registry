[
  {
    "name": "nucleus_engrams",
    "description": "Persistent memory: write, query, search, health, version, morning brief",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (write, query, search, health, version, morning_brief)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  },
  {
    "name": "nucleus_governance",
    "description": "Governance, Hypervisor & security tools: lock, unlock, watch, audit, compliance, curl proxy",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (lock, unlock, set_mode, watch, status, curl, pip_install, validate_strategic_plan, comply_list, auto_fix_loop)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  },
  {
    "name": "nucleus_align",
    "description": "ALIGN — Human correction frontier. Every correction trains the system.",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (correct, approve, stats)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  },
  {
    "name": "nucleus_ground",
    "description": "GROUND — Execution verification. Tiered verification on git changes (syntax, imports, tests, runtime).",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (verify, receipt, hook)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  },
  {
    "name": "nucleus_tasks",
    "description": "Task management, depth tracking & ADHD context-switch tools",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (list, get_next, claim, update, add, import_jsonl, escalate, depth_push, depth_pop, depth_show, depth_reset, depth_set_max, depth_map)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  },
  {
    "name": "nucleus_sessions",
    "description": "Session management, events, state & checkpoint tools",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (save, resume, list, check_recent, end, start, archive_resolved, propose_merges, garbage_collect, emit_event, read_events, get_state)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  },
  {
    "name": "nucleus_sync",
    "description": "Multi-agent sync, artifact management, triggers & deploy coordination",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (identify_agent, sync_status, sync_now, sync_auto, sync_resolve, read_artifact, write_artifact, list_artifacts, trigger_agent, get_triggers, evaluate_triggers, start_deploy_poll)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  },
  {
    "name": "nucleus_orchestration",
    "description": "Satellite view, commitments, loops, patterns & metrics",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (satellite, scan_commitments, archive_stale, export, list_commitments, close_commitment, commitment_health, open_loops, add_loop, weekly_challenge, patterns, metrics)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  },
  {
    "name": "nucleus_archive",
    "description": "Training pipeline: SFT/DPO pair generation, deltas, frontier health",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (record_sft, record_dpo, record_delta, frontier_health, list_pairs, export_pairs)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  },
  {
    "name": "nucleus_telemetry",
    "description": "LLM tiers, telemetry, PEFS notifications & protocol tools",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (set_llm_tier, get_llm_status, record_interaction, value_ratio, check_kill_switch, pause_notifications, resume_notifications, record_feedback, check_protocol, request_handoff, get_handoffs, agent_cost_dashboard, dispatch_metrics, rate_limit_status)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  },
  {
    "name": "nucleus_slots",
    "description": "Orchestration slots, sprints & mission management",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (orchestrate, slot_complete, slot_exhaust, status_dashboard, autopilot_sprint, force_assign, start_mission, mission_status, halt_sprint, resume_sprint)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  },
  {
    "name": "nucleus_infra",
    "description": "Infrastructure: file changes, cloud services, marketing & strategy tools",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (file_changes, gcloud_status, gcloud_services, list_services, scan_marketing_log, synthesize_strategy, status_report, optimize_workflow, manage_strategy, update_roadmap, growth_pulse, capture_metrics)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  },
  {
    "name": "nucleus_federation",
    "description": "Federation: join, leave, peers, route, health for multi-brain coordination",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (join, leave, peers, route, health)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  },
  {
    "name": "nucleus_features",
    "description": "Feature map, proofs & mounter for feature tracking and verification",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (add, list, mount, proof)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  },
  {
    "name": "nucleus_agents",
    "description": "Agent spawning, critic, swarm, memory, ingestion & dashboard tools",
    "arguments": [
      {"name": "action", "type": "string", "desc": "Action to perform (spawn_agent, apply_critique, orchestrate_swarm, search_memory, read_memory, critique_code, fix_code, session_briefing, register_session, handoff_task, ingest_tasks, rollback_ingestion, ingestion_stats, dashboard, snapshot_dashboard, get_alerts, set_alert_threshold)"},
      {"name": "params", "type": "object", "desc": "Action-specific parameters"}
    ]
  }
]
